import SupportingText from '../components/mdl-card-supporting-text';
import layout from '../templates/components/mdl-card-header';

export default SupportingText.extend({
  layout,

  classNames: ['mdl-card__header']
});
