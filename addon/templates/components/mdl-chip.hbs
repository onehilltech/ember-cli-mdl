{{#if contact}}
  <span class="mdl-chip__contact">{{contact}}</span>
{{/if}}

<span class="mdl-chip__text">{{text}}</span>
{{#if deletable}}
  <button type="button" class="mdl-chip__action" {{action "delete"}}><i class="material-icons">{{deleteIcon}}</i></button>
{{/if}}
